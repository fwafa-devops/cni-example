# Custom values for my-app-2 application
# This demonstrates GitOps upgrades and rollbacks with configurable features

# Basic configuration
replicaCount: 1

# Image settings - use kodekloud/webapp-color
image:
  repository: kodekloud/webapp-color
  tag: "latest"
  pullPolicy: IfNotPresent

# Container configuration
containerPort: 8080

# Color selector - change this to switch colors instantly!
# The webapp-color container accepts: red, green, blue, darkblue, pink
appColor: "pink"

# Service configuration - NodePort with specific port for reliable access
service:
  type: NodePort
  port: 80
  nodePort: 30083  # Specific NodePort for easy access

# Resource limits
resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
